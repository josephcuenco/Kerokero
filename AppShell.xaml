<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="KeroKero.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:KeroKero.Pages"
    xmlns:view="clr-namespace:KeroKero.ViewModels"
    Shell.FlyoutBehavior="Disabled"
    Title="KeroKero">

    <!-- Home, Login, and SignUp pages can be defined here if needed for navigation -->
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage" />

    <ShellContent
        Title="SignUp"
        ContentTemplate="{DataTemplate local:SignUpPage}"
        Route="SignUpPage" />

    <!-- TabBar for the bottom navigation bar -->
    <TabBar>
        <ShellContent Title="Map" ContentTemplate="{DataTemplate local:MapPage}" Route="MapPage"/>
        <ShellContent Title="Info" ContentTemplate="{DataTemplate local:InfoPage}" Route="InfoPage"/>
    </TabBar>

</Shell>

